# Rust / Anchor
/target
**/target
Cargo.lock
.anchor
test-ledger
**/*.rs.bk

# Solana keypairs / secrets — never commit
*.pem
**/deploy-keypair.json
.env
.env.*
!.env.example

# Node / Next.js
node_modules
.next
next-env.d.ts
out
dist
build
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Generated IDLs / types are produced by `anchor build` + scripts/initialize.ts.
# Placeholder stubs are committed so the frontend builds on a fresh clone;
# real output overwrites them locally (do not commit real deploy secrets).

# Editor / OS
.DS_Store
.idea
.vscode
*.log
